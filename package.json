{
  "name": "Petits Plats",
  "devDependencies": {
    "amd-loader": "^0.0.8"
  },
  "scripts": {
    "build": "esbuild scripts/app.ts --bundle --outfile=dist/app.js --watch"
  },
  "dependencies": {
    "esbuild": "^0.19.5",
    "typescript": "^5.2.2"
  }
}
